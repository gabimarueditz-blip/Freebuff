# Freebuff

A lightweight neighborhood exchange app for giving away useful things and finding free items nearby.

## Run it

Start it from the repository with:

```sh
npm start
```

Then visit `http://localhost:8000`.

You can also open `index.html` directly, or serve the folder with any static file server:

```sh
python3 -m http.server 8000
```
